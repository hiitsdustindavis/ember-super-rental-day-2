{{#if addNewCity}}
  <h1>New Rental</h1>
      <div>
        <form>
          <div class="form-group">
            <label for="owner">Owner</label>
            {{input value=owner id="owner"}}
          </div>

          <div class="form-group">
            <label for="type">Type</label>
            {{input value=type id="type"}}
          </div>

          <button {{action 'addNewCity1'}}>Save</button>
        </form>
      </div>
  {{else}}
    <button {{action 'cityFormShow'}}>New Rental</button>
{{/if}}
